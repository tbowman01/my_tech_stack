apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-template.fullname" . }}
  labels:
    {{- include "svc-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "svc-template.selectorLabels" . | nindent 4 }}